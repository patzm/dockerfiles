http:
  routers:
    traefik-websecure:
      entrypoints: websecure
      rule: "Host(`traefik.patz.app`)"
      middlewares:
        - auth
      tls:
        certResolver: patz.app
        domains:
          - main: "patz.app"
            sans:
              - "traefik.patz.app"
      service: api@internal
  middlewares:
    auth:
      basicAuth:
        users:
          - "admin:$apr1$3XFrbaba$UjpbdjwdD6ie9eqGcHlou/"

